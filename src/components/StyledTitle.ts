import styled from "styled-components";

const StyledTitle = styled.h1`
    text-align: center;
    color: rgba(0, 79, 20, 0.46);
    text-decoration: underline; /* from stack overflow */
`;

export default StyledTitle;